# Paired-Sampling Contrastive Framework
# https://arxiv.org/abs/2508.14980

# Core ML
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# Configuration & utilities
mmengine>=0.10.0
cosine_annealing_warmup @ git+https://github.com/katsura-jp/pytorch-cosine-annealing-with-warmup

# Data processing
numpy>=1.24.0
pandas>=2.0.0
opencv-python-headless>=4.8.0
albumentations>=1.3.0
scikit-learn>=1.3.0

# Progress & logging
tqdm>=4.65.0
tabulate>=0.9.0
wandb>=0.15.0  # optional, for experiment tracking

# Face detection (for data preparation only)
facenet-pytorch>=2.6.0  # optional, for prepare_data scripts
